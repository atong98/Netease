//获取当前关键字的异步action，调用接口请求函数发请求，有结果提交
// import {reqKeywordSearch} from '../api/index'
// import { RECEIVE_SEARCH } from './mutation-types';

export default{
  // async getKeywordSearch({commit},info){
  //   const result = await reqKeywordSearch(info)
  //   const search = result.data
  //   commit(RECEIVE_SEARCH,search)
  // }
}

